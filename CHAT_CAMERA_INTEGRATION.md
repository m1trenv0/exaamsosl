# 📸 Камера в чате - Инструкция

## Что добавлено

В компонент чата во время экзамена ([ChatQuestion.tsx](components/exam/question-types/ChatQuestion.tsx)) добавлен функционал захвата фото с камеры.

### 🎯 Расположение

Кнопка камеры появляется в чате **слева от поля ввода сообщения**.

### 📱 Как пользоваться (для студента)

1. **Откройте чат** - он становится видимым на определённом вопросе (когда громкость 45-55)
2. **Нажмите кнопку камеры** 📷 (слева от поля ввода)
3. **Разрешите доступ к камере** в браузере
4. **Сделайте фото** - нажмите "Capture"
5. **Проверьте preview**:
   - Если фото хорошее → "Send Photo"
   - Если нужно переснять → "Retake"
6. Фото отправится инструктору

### 👨‍🏫 Как видит инструктор

- Фото отображается в админ-панели чата
- Можно кликнуть по фото для открытия в новой вкладке
- Можно скачать (ПКМ → Сохранить изображение)

### 🔧 Технические детали

**Компоненты:**
- `ChatQuestion.tsx` - основной чат в интерфейсе экзамена
- `CameraCapture.tsx` - компонент захвата фото
- `ChatManager.tsx` - админская панель (уже поддерживает фото)

**Хранение:**
- Изображения сохраняются в базе данных как base64
- Поле `image_data` в таблице `chat_messages`
- Автоматическая компрессия JPEG (качество 80%)

**API:**
- POST `/api/chat` с полем `image_data`
- Сообщение автоматически помечается как "📷 Photo"

### ✅ Статус

- ✅ Сборка успешна
- ✅ Линтер пройден
- ✅ TypeScript без ошибок
- ✅ Готово к использованию

### 🎨 Дизайн

Кнопка камеры стилизована в соответствии с общим дизайном чата:
- Outline стиль кнопки
- Иконка камеры из lucide-react
- Деактивируется во время отправки

---

**Дата внедрения:** 6 января 2026
